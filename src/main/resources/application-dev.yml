spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lifeos
    username: postgres
    password: asdfghjkl

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true